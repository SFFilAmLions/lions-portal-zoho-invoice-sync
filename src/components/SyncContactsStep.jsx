// eslint-disable-next-line no-unused-vars
export default function SyncContactsStep({
  membersData,
  selectedOrgId,
  onComplete,
}) {
  return (
    <div>
      <p>Step 3: Sync Contacts (stub)</p>
      <button onClick={onComplete}>Continue (stub)</button>
    </div>
  )
}
