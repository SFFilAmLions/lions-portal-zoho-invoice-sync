// eslint-disable-next-line no-unused-vars
export default function ConnectZohoStep({ onComplete, onOrgSelected }) {
  return (
    <div>
      <p>Step 2: Connect to Zoho (stub)</p>
      <button onClick={onComplete}>Continue (stub)</button>
    </div>
  )
}
